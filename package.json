{
  "name": "@geee-be/libraries",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "license": "MIT",
  "author": "Greg Bacchus",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter core build && pnpm --filter service build && pnpm --filter logger build && pnpm --filter api build && pnpm --filter migrate build && pnpm --filter vitest-bdd build && pnpm --filter create build",
    "changes": "changeset && git add .changeset",
    "changeset": "changeset",
    "lint": "pnpm --stream -r lint",
    "lint:biome": "pnpm --stream -r lint:biome",
    "start": "pnpm --stream -r start",
    "start:dev": "pnpm --stream --parallel -r start:dev"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "@changesets/cli": "2.26.2",
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.56",
    "@types/node": "^20.1.0",
    "chokidar": "^3.5.3",
    "esbuild": "^0.17.18",
    "rimraf": "^5.0.0",
    "ts-unused-exports": "^9.0.4",
    "typescript": "^5.4.2"
  }
}
